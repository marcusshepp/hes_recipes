<%= form_for @step, :url =>{ :action => "create", :id =>@step.id }, :html => {:onsubmit => "return val_step();"}  do |i| %>
    <p>
        <div class="">
            This is a Step for which Recipe:
        </div>
        <select class="" name="recipe_id">
            <% Recipe.all.each do |r| %>
                <% if r.id == params[:recipe_id].to_i %>
                    <option value="<%= r.id %>" selected="selected"><%= r.title %></option>
                <% else %>
                    <option value="<%= r.id %>"><%= r.title %></option>
                <% end %>
            <% end %>
        </select>
    </p>
    <div class="step_num_val"></div>
    <p>Step Number: <%= i.number_field  'sequence', :class => 'step_num' %></p>
    <div class="desc_val"></div>
    <p>Description: <%= i.text_field  'description', :class => 'description' %></p>
    <%= i.submit "Save And Continue" %>
    <%= i.submit "Save and Add Another Step" %>
<% end %>
